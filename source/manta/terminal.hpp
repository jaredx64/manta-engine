#pragma once

#include <config.hpp>

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#ifdef COMPILE_TERMINAL
	#define TERMINAL_ENABLED ( COMPILE_TERMINAL )
#else
	#define TERMINAL_ENABLED ( false )
#endif

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

namespace CoreTerminal
{
	bool init();
	bool free();
	void update();
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

namespace Terminal
{
	void get_dimensions( int &columns, int &rows );

	void clear();
	void clear_line();
	void clear_to_end();

	void cursor_move( int column, int row );
	void cursor_move_top_left();
	void cursor_save();
	void cursor_restore();
	void cursor_hide();
	void cursor_show();

	void write( const char *text );
	void flush();
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////