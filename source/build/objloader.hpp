#pragma once

#include <core/types.hpp>

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

// TODO: .obj model loading will likely be removed from the build tool. This and associated code is temporary!

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

struct GfxBuiltInVertex
{
	float x, y, z;
	u16 u, v;
	u8 r, g, b, a;
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

struct MeshObj
{
	bool load( const char *path );
	bool free();

	GfxBuiltInVertex *vertexBufferData = nullptr;
	usize vertexBufferSize = 0;
	usize vertexCount = 0;

	u32 *indexBufferData = nullptr;
	usize indexBufferSize = 0;
	usize indexCount = 0;
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////